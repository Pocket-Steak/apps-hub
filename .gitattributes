# Auto detect text files and perform LF normalization
* text=auto

# Treat all image and binary formats as binary — never mangle line endings
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.svg binary
